apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  namespace: argocd
data:
  # Local-only convenience: terminate TLS at ingress with plain HTTP to argocd-server.
  server.insecure: "true"
