apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
data:
  # Very fast polling for local/demo environments.
  timeout.reconciliation: "5s"
  # Remove random delay to keep sync latency predictable.
  timeout.reconciliation.jitter: "0s"
